init offset = -2
init python:
    settings = {}
    settings["tilesize"] = 48.0 #in pixel
    settings["resolution"] = (1080.0,720.0)

    import math
    import random
    import pygame
    import copy
